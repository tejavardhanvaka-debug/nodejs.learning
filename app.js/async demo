const fetchData = () => {
    return new Promise((resolve) => {
        setTimeout(() => resolve("Data received"), 2000);
    });
};

async function run() {
    const data = await fetchData();
    console.log(data);
}

run();